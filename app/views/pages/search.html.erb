<h1>THIS IS WHERE THE SEARCH WILL HAPPEN</h1>

<h2>---------USER INPUT AND OUTPUT---------</h2>

<%= render "user_input" %>
<%= render "output" %>

<h2>---------USERS---------</h2>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to "#{user.first_name} #{user.last_name}", user_path(user) %> - <%= user.location %> - <%= user.role %>
    </li>
  <% end %>
</ul>
