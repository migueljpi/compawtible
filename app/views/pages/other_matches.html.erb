<h1>OTHER MATCHES</h1>

<h2>Input: <%= @prompt.input %></h2>

<ul>
  <% @best_matches.each do |match| %>

    <li><%= link_to match.name, user_pet_path(match.provider.id, match.id) %> - <%= match.breed %> - <%= match.species %> - <%= match.description %> - <%= match.location %></li>
  <% end %>
</ul>
